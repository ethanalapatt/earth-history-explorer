{
  "name": "earth-history-explorer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "node server/index.js --dev",
    "build": "vite build",
    "start": "node server/index.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
